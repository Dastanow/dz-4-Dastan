function findSmallerNumber(num1, num2) {
    if (num1 < num2) {
      return num1;
    } else {
      return num2;
    }
  }
  
  const result = findSmallerNumber(5, 10);
  
  console.log(result); 
  function power(base, exponent) {
    return Math.pow(base, exponent);
  }
  
  const resul = power(2, 3);
  console.log(resul);